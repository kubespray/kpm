input {
       kinesis {
         kinesis_stream_name => "{{stream_name}}"
         # metrics => "cloudwatch"
         checkpoint_interval_seconds => 10
         application_name => "{{stream_name}}"
         # TODO(ant31): add type
         # codec => json { }
       }
      }
